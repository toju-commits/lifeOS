# LifeOS

Vite React app for the JoshuaOS War Room canvas component.

## Local Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The app is ready to deploy to Vercel with the default Vite settings:

- Build command: `npm run build`
- Output directory: `dist`
